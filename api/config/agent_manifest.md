# API Config Agent Manifest
This folder contains configuration data for the backend quoting engine.

## Files
- `pricing.json`: A JSON configuration file holding default parameters, price lists for static catalog items (wall panels, floor systems, kitchens, bathrooms, etc.), logic assumptions (waste factors, labor costs, overhead/profit markups), and scraping targets for live market prices (e.g., Sodimac URLs for steel frames).
